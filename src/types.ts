export type Video = {
    id: string;
    src: string;
    title?: string;
    description?: string;
    tags?: string[];
    autoplay?: boolean;
    loop?: boolean;
    controls?: boolean;
    width?: string;
    height?: string;
  };
  